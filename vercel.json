{
  "version": 2,
  "installCommand": "npm install --omit=dev",
  "functions": {
    "server.mjs": {
      "maxDuration": 300,
      "memory": 1024,
      "includeFiles": "{public/**,lib/data/**}"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/server.mjs" }
  ]
}
